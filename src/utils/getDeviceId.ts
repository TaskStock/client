// Expo에서는 지원 안함. eject 후 사용
// import DeviceInfo from "react-native-device-info";

// const getDeviceId = async () => {
//   const deviceId = DeviceInfo.getUniqueId();
//   console.log("====== Device ID: ", deviceId);
//   return deviceId;
// };

// export default getDeviceId;
